#include <stdlib.h>
#include <stdio.h>
/**
 * main - entry point. Prints number of args passed into it
 * @argc: number of command line args
 * @argv: array of command ine args
 *
 * Return: Success
 */
int main(int argc, __attribute__((unused)) char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
