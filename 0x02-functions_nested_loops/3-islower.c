#include "main.h"
/**
 * _islower - checks if a character is a lower case or not
 * @c: The character to test
 *
 * Return: 1 if the char is lower or 0 if not (Success)
 */
int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
